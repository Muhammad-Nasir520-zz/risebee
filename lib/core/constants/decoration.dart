import 'package:flutter/material.dart';

final preRegisterTextFieldDeco = InputDecoration(
    hintText: 'Name / Surname',
    hintStyle: TextStyle(color: Color(0xFFACACAC),fontSize: 17),
    border: InputBorder.none
);

final topRightLeftRadius = BorderRadius.only(topRight: Radius.circular(10),topLeft: Radius.circular(15));